# Core dependencies
pydantic>=2.0.0
lancedb==0.22.0
docling==2.31.0
cohere==5.15.0

# LLM providers — install whichever you need (at least one required)
openai>=1.0.0        # LLM_PROVIDER=openai   (default)
anthropic>=0.40.0    # LLM_PROVIDER=anthropic
httpx>=0.27.0        # LLM_PROVIDER=ollama
